module.exports = function() {
    var keywords = {};

    // Positive
    keywords['62'] = [];

    // Teamwork
    keywords['63'] = [];

    // Open
    keywords['64'] = [];

    // Confident
    keywords['65'] = [];

    // Sincere
    keywords['66'] = [];

    // optimism
    keywords['77'] = [
        'yêu đời', 'lạc quan', 'vui', 'duoc ngay'
    ];

    // active
    keywords['78'] = [
        'thể thao', 'bóng rổ', 'bóng đá', 'lễ hội', 'sự kiện',
        'event', 'tổ chức', 'hoạt động', 'tình nguyện', 'tham gia'
    ];

    // confidence
    keywords['79'] = [
        'tự tin', 'mình nghĩ', 'bạn nên', 'minh cho là', 'mình cho rằng'
    ];

    // communication
    keywords['80'] = [
        'ơi', 'mày', 'giúp'
    ];

    // funny
    keywords['81'] = [
        'vui', 'ha ha', 'được đó', 'vãi', 'troll', ':D', ':v', '^_^', 'lol', 'cool', 'nice'
    ];

    return keywords;
}();